# Dependencias
node_modules/

# Carpeta generada por Capacitor: se rearma con `npm run preparar`.
# Versionarla mete miles de archivos que no son trabajo propio y ensucian
# la historia de commits, que es justamente lo que el docente evalúa.
android-build/www/
android-build/android/
android-build/node_modules/

# Salidas de compilación
*.apk
*.aab
*.keystore

# Sistema operativo
Thumbs.db
Desktop.ini
.DS_Store

# Editores
.vscode/
.idea/
*.swp

# Configuración de herramientas del asistente de programación (gentle-ai).
# Son 75 archivos de tooling que no son trabajo propio del TP: mismo
# criterio que android-build/, no ensuciar la historia que evalúa el
# docente. Se regeneran con `gentle-ai install --scope workspace`.
.claude/

# Temporales
*.log
*.tmp
